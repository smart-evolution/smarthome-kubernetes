# Smarthome Kubernetes Scripts

Prepated only for Debian distro

### How to install
1. Create with `sudo` `/etc/init.d/shkubernetes`
2. Copy `bootsrap.sh` to `/etc/init.d/shkubernetes`
3. `sudo chmod +x /etc/init.d/shkubernetes/bootsrap.sh`
